# spring.application.name=job-quest-backend
# server.port=${PORT:8080}
# spring.data.mongodb.database=${MONGO_DATABASE}
# spring.data.mongodb.uri=mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}
# jwt.secret=${JWT_SECRET_KEY}
# jwt.expiration=${JWT_EXPIRATION}
spring.application.name=job-quest-backend
server.port=8080


# Kết nối MongoDB local (Docker)
spring.data.mongodb.database=jobquest
spring.data.mongodb.uri=mongodb://root:yourpassword@localhost:27017/jobquest

# JWT config
jwt.secret=mySuperSecretKey
jwt.expiration=86400000


logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
# logging.level.org.mongodb.driver=INFO


